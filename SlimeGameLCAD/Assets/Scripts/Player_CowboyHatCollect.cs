using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player_CowboyHatCollect : MonoBehaviour
{
    private Rigidbody2D myRigidbody;

    public int hat;
    private int hatobtained = 0;

    public GameObject winScreen;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //hat floats up and down
        //hat animates to sparkle?

        //item disappears on contact
        //void has hat(has hat screen yay, scene change, player has hat)
        
        
    }

    void hasHat()
    {
        //yay has hat screen
        //change scene, but with hat
    }
}
